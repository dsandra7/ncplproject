echo "# ncplazuregit" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/rajcocvs/ncplazuregit.git
git push -u origin master



[ ] Create a new Git repository on GitHub and clone it to the local machine (Raj)
[ ] Initialize a new Git repository in a local folder and add files to it (Raj)
[ ] Add files to the staging area using the `git add` command (Raj)
[ ] Commit the staged changes to the local repository using the `git commit` command (Raj)
[ ] Push the committed changes from the local repository to the remote GitHub repository using the `git push` command (Raj)

See full summary - h
rajco siva
10:01
git config --global user.name "ncpl"
git config --global user.email ""ncpl@gmail.com